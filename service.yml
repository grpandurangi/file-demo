apiVersion: v1
kind: Service
metadata:
  labels:
    name: frontend
  name: frontend
spec:
  type: LoadBalancer
  LoadBalancerIP: 35.186.255.149
  ports:
    # The port that this service should serve on.
    - port: 9090
      targetPort: 9090
      protocol: TCP
  # Label keys and values that must match in order to receive traffic for this service.
  selector:
    app: "my-file-demo"
